html5
html
    head
        title= pageTitle
    body
        header
            h1= pageTitle
        section.login
            ul
                if session.isLoggedIn
                    li
                        a(href="/logout") Logout
                else
                    li
                        a(href="/login") Login
        section.content
            block content