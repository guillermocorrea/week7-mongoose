extends layout

block content
    if !(posts && posts.length)
        p Nothing to see here. Move a long...
    else
        ul.posts
            each post in posts
                li
                    h2
                        a(href="/posts/#{post.id}")= post.title